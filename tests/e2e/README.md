# End-to-End Tests

Future Playwright or browser-level tests for the core demo flow.

Primary flow to cover:

```text
connect wallet -> create transfer -> agent confirms -> receipt -> claim page
```

