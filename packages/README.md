# Packages

Workspace-style packages for shared domain logic and reusable UI.

Suggested layout:

```text
shared/  Transfer types, route helpers, constants
ui/      Reusable presentation components if the app grows
```

