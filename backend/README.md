# Backend

This folder is reserved for backend services if the MVP needs API routes outside the Next.js app.

Potential responsibilities:

```text
Transfer persistence
Agent confirmation APIs
Webhook handlers
Pollar server-side calls
Receipt generation
```

For fastest delivery, prefer Next.js route handlers first.

